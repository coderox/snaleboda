<views:MvxWindowsPage
    x:Class="Snaleboda.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Snaleboda"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Cirrious.MvvmCross.WindowsCommon.Views"
    mc:Ignorable="d">

    <views:MvxWindowsPage.Resources>
        <CollectionViewSource x:Name="newsCollection" Source="{Binding News}" IsSourceGrouped="False"/>
        <CollectionViewSource x:Name="contactsCollection" Source="{Binding Contacts}" IsSourceGrouped="False"/>
    </views:MvxWindowsPage.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Hub>

            <HubSection Header="välkommen">
                <DataTemplate>
                    <TextBlock Text="{Binding Hello}"/>
                </DataTemplate>
            </HubSection>

            <HubSection Header="nyheter">
                <DataTemplate>
                    <Grid>
                        <ListView x:Name="newsList"
                                  ItemsSource="{Binding Source={StaticResource newsCollection}}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </DataTemplate>
            </HubSection>

            <HubSection Header="kontakter">
                <DataTemplate>
                    <Grid>
                        <ListView x:Name="contactsList"      
                                  ItemsSource="{Binding Source={StaticResource contactsCollection}}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </DataTemplate>
            </HubSection>

        </Hub>
    </Grid>

    <views:MvxWindowsPage.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="rapportera" Icon="Stop"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </views:MvxWindowsPage.BottomAppBar>
</views:MvxWindowsPage>
